# 📖 دليل المدخلات والمخرجات والبروتوكولات
# SAQR Seeker / CANphon Reference Manual

**الإصدار**: 1.0  
**التاريخ**: 2026-01-12  
**المشروع**: SAQR Seeker - نظام التتبع والتحكم الروبوتي

---

# فهرس المحتويات

1. [مقدمة عامة](#الفصل-1-مقدمة-عامة)
2. [المدخلات (Inputs)](#الفصل-2-المدخلات-inputs)
3. [المخرجات (Outputs)](#الفصل-3-المخرجات-outputs)
4. [البروتوكولات التفصيلية](#الفصل-4-البروتوكولات-التفصيلية)
5. [مخططات التدفق](#الفصل-5-مخططات-التدفق)
6. [الملاحق](#الملاحق)

---

# الفصل 1: مقدمة عامة

## 1.1 نظرة عامة على النظام

نظام **SAQR Seeker** هو إطار عمل روبوتي عسكري المستوى يعمل بمعمارية **"الهاتف كعقل" (Smartphone-as-Brain)**. يستخدم هاتف Android كوحدة معالجة مركزية للكشف عن الأهداف، التتبع المستمر، والتحكم الدقيق في السيرفوهات والجيمبال.

### المكونات الرئيسية:
- **YOLOv8**: كشف الأهداف بالذكاء الاصطناعي
- **ByteTrack**: تتبع متعدد الأهداف
- **Kalman Filter**: تنبؤ حركة الأهداف
- **PID Controller**: تحكم دقيق في الموقع
- **CAN/Serial Bus**: اتصال مع السيرفوهات

## 1.2 معمارية "الهاتف كعقل"

```
┌─────────────────────────────────────────────────────────────────┐
│                     📱 Android Brain                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐    │
│  │ Sensors  │→ │ AI/ML    │→ │ Tracking │→ │ PID Control  │    │
│  │ Input    │  │ Detection│  │ Engine   │  │ + Decision   │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────┘    │
└─────────────────────────────────────────────────────────────────┘
        ↓                                              ↓
   📡 Telemetry                                   ⚙️ Actuators
   (Python GCS)                                   (Servos/Motors)
```

## 1.3 مستوى التوافق العسكري

| الميزة | الحالة | الملاحظات |
|--------|--------|-----------|
| البنية الطبقية | ✅ | 4 طبقات دمج |
| التتبع الهجين | ✅ | ByteTrack + Kalman |
| إعادة التعريف | ✅ | Color-based Re-ID |
| Human-in-Loop | ✅ | إلزامي للاشتباك |
| دمج حساسات متعددة | ✅ | Camera 70% + IMU 20% + GPS 10% |

---

# الفصل 2: المدخلات (Inputs)

## 2.1 الكاميرا (CameraX)

### المواصفات الفنية
| المعيار | القيمة |
|---------|--------|
| **الدقة** | 1080p (1920×1080) أو 720p (1280×720) |
| **معدل الإطارات** | 30 FPS |
| **التنسيق** | YUV_420_888 |
| **المكتبة** | Android CameraX |
| **الاستراتيجية** | `STRATEGY_KEEP_ONLY_LATEST` |

### تنسيق البيانات
```kotlin
data class CameraFrame(
    val imageProxy: ImageProxy,      // إطار YUV
    val timestamp: Long,             // الوقت بالنانو ثانية
    val rotationDegrees: Int,        // 0, 90, 180, 270
    val width: Int,                  // 1920 أو 1280
    val height: Int                  // 1080 أو 720
)
```

### الاستخدام
- **الكشف بالذكاء الاصطناعي**: إطار واحد كل 3-10 إطارات
- **التتبع**: كل إطار (30 Hz)
- **التسجيل**: MP4 بدقة كاملة

---

## 2.2 الجيروسكوب والتسارع (IMU)

### المواصفات الفنية
| المعيار | القيمة |
|---------|--------|
| **مصدر البيانات** | Android Sensor API |
| **نوع الحساس** | `TYPE_GYROSCOPE` + `TYPE_ACCELEROMETER` |
| **معدل البيانات** | 100+ Hz (`SENSOR_DELAY_GAME`) |
| **الوحدات** | rad/s (Gyro) | m/s² (Accel) |

### تنسيق البيانات
```kotlin
data class IMUData(
    val gyroX: Float,    // rad/s - الدوران حول X
    val gyroY: Float,    // rad/s - الدوران حول Y
    val gyroZ: Float,    // rad/s - الدوران حول Z
    val accelX: Float,   // m/s² - التسارع على X
    val accelY: Float,   // m/s² - التسارع على Y
    val accelZ: Float,   // m/s² - التسارع على Z
    val timestamp: Long  // نانو ثانية
)
```

### الوظيفة
- **تعويض الاهتزاز**: تثبيت الصورة/التتبع
- **كشف الميلان**: تحديد وضعية الجهاز
- **Grace Period**: الحفاظ على التتبع عند فقدان الهدف مؤقتاً

---

## 2.3 GPS الداخلي (Android Location)

### المواصفات الفنية
| المعيار | القيمة |
|---------|--------|
| **المصدر** | Android Location Manager |
| **الدقة** | 3-10 أمتار (حسب الظروف) |
| **معدل التحديث** | 1-10 Hz |
| **الصلاحية المطلوبة** | `ACCESS_FINE_LOCATION` |

### تنسيق البيانات
```kotlin
data class PhoneGPSData(
    val latitude: Double,      // خط العرض
    val longitude: Double,     // خط الطول
    val altitude: Double,      // الارتفاع (متر)
    val speed: Float,          // السرعة (م/ث)
    val bearing: Float,        // الاتجاه (درجة)
    val accuracy: Float,       // دقة الموقع (متر)
    val satellites: Int,       // عدد الأقمار
    val timestamp: Long        // الوقت
)
```

---

## 2.4 GPS الخارجي (KCA Serial)

### المواصفات الفنية
| المعيار | القيمة |
|---------|--------|
| **الاتصال** | USB Serial (RS232-to-TTL) |
| **معدل البود** | 115200 bps |
| **حجم الحزمة** | 164 بايت |
| **الدقة** | 0.5-2 متر (Multi-Constellation) |
| **معدل التحديث** | 10 Hz |

### هيكل الحزمة (164 بايت)
| البايت | الحقل | الوصف |
|--------|-------|-------|
| 0 | Sync 1 | `0x81` |
| 1 | Sync 2 | `0x7E` |
| 2-161 | NavData | 160 بايت بيانات الملاحة |
| 162-163 | CRC | CRC-16 CCITT |

### حقول NavData الرئيسية
| الموقع | الحقل | النوع | الوصف |
|--------|-------|-------|-------|
| 1 | State | uint8 | حالة الـ Fix (0=غير جاهز، 1+=صالح) |
| 72-75 | Latitude | float | خط العرض |
| 76-79 | Longitude | float | خط الطول |
| 80-83 | Altitude | float | الارتفاع |
| 90-101 | Velocity | float[3] | $V_x, V_y, V_z$ ECEF |
| 131-135 | DOPs | uint8[5] | GDOP, PDOP, HDOP, VDOP, TDOP |

### مثال على البيانات
```
Header: 81 7E
State: 03 (3D Fix)
Latitude: 24.7136° N
Longitude: 46.6753° E
Altitude: 612.5 m
Satellites: 12
HDOP: 0.8
CRC: Valid ✓
```

---

## 2.5 التغذية الراجعة من السيرفو (Servo Feedback)

### المواصفات الفنية
| المعيار | القيمة |
|---------|--------|
| **البروتوكول** | STM32 Unified 7-Byte |
| **معدل البود** | 115200 bps |
| **معدل التحديث** | 50-100 Hz |
| **الدقة** | 0.025° |

### هيكل حزمة الفيدباك (7 بايت)
| البايت | الحقل | الوصف |
|--------|-------|-------|
| 0 | Sync+ID_H | `0x80 | OpCode | (ID>>7 & 0x03)` |
| 1 | ID_Low | `ID & 0x7F` |
| 2 | Data_H | البيانات العليا |
| 3 | Data_L | البيانات الدنيا |
| 4 | Extra1 | محجوز |
| 5 | Extra2 | محجوز |
| 6 | Checksum | `((XOR 0..5) & 0x7F) | 0x40` |

### تحويل البيانات
```kotlin
// تحويل الموقع الخام إلى زاوية
val rawPosition = (byte2 * 128) + byte3
val angleDegrees = (rawPosition - 8191) * 0.025f

// تحويل درجة الحرارة
val rawTemp = (byte2 * 128) + byte3
val tempCelsius = (rawTemp - 8191) * 0.1f
```

---

## 2.6 إدخال المستخدم (User Input)

### أحداث اللمس
| الحدث | الوظيفة |
|-------|---------|
| **نقرة واحدة** | تحديد هدف جديد |
| **نقرة مزدوجة** | إلغاء التتبع |
| **سحب** | تحريك يدوي للجيمبال |
| **ضغط مطول** | قفل/فتح الهدف |

### أوامر التحكم
```kotlin
data class ControlCommand(
    val throttle: Int,    // 0-1000
    val roll: Int,        // -500 to +500
    val pitch: Int,       // -500 to +500
    val yaw: Int          // -500 to +500
)
```

---

# الفصل 3: المخرجات (Outputs)

## 3.1 أوامر السيرفو - CAN Bus

### المواصفات الفنية
| المعيار | القيمة |
|---------|--------|
| **البروتوكول** | XQPOWER v022 |
| **معدل البود** | 2 Mbps |
| **المحول** | Waveshare USB-CAN-A (VID: 0x1A86) |
| **نطاق الزاوية** | -100.0° إلى +100.0° |
| **الدقة** | 0.1° |
| **معدل الإرسال** | 50 Hz |

### هيكل إطار CAN
```
[HEAD: 0xAA][ID_LOW][ID_HIGH][DLC][DATA_0..7][CHECKSUM]
```

### أوامر XQPOWER
| الأمر | الوظيفة | البيانات |
|-------|---------|----------|
| `0x01` | تعيين الموقع | الزاوية × 10 (int16) |
| `0x02` | تعيين السرعة | RPM × 10 |
| `0x05` | قراءة الفيدباك | الموقع + السرعة + الحرارة |

### مثال على أمر الموقع
```kotlin
// تحريك السيرفو ID=601 إلى زاوية 45.5°
val angleValue = (45.5 * 10).toInt()  // = 455
// CAN Frame: [0xAA][0x59][0x02][0x02][0xC7][0x01][CS]
```

---

## 3.2 أوامر السيرفو - Serial (STM32 5-Byte)

### المواصفات الفنية
| المعيار | القيمة |
|---------|--------|
| **حجم الحزمة** | 5 بايت |
| **معدل البود** | 115200 bps |
| **نطاق الزاوية** | -25.0° إلى +25.0° |
| **الدقة** | 0.025° (14-bit) |
| **الوحدات لكل درجة** | 40.0 |
| **القيمة المحايدة (0°)** | 8191 |

### هيكل الحزمة (5 بايت)
| البايت | الاسم | المنطق |
|--------|-------|--------|
| 0 | Sync+OpCode | `0x80 | 0x08 | ((servoId >> 7) & 0x03)` |
| 1 | ID | `servoId & 0x7F` |
| 2 | Hpos | `(position >> 7) & 0x7F` |
| 3 | Lpos | `position & 0x7F` |
| 4 | Checksum | `(Byte0 ^ Byte1 ^ Byte2 ^ Byte3) & 0x7F` |

### صيغة التحويل
```kotlin
// تحويل الزاوية إلى قيمة الموقع
fun angleToPosition(angleDegrees: Float): Int {
    return ((angleDegrees * 40.0f) + 8191).toInt()
        .coerceIn(0, 16383)
}

// مثال: 10° → Position = (10 * 40) + 8191 = 8591
```

### مثال على حزمة
```
// تحريك Servo ID=1 إلى 10°
Position = 8591 = 0x218F
Byte0 = 0x88  (0x80 | 0x08 | 0x00)
Byte1 = 0x01  (ID)
Byte2 = 0x43  ((8591 >> 7) & 0x7F = 67)
Byte3 = 0x0F  (8591 & 0x7F = 15)
Byte4 = 0x45  (XOR checksum)

Packet: [88 01 43 0F 45]
```

---

## 3.3 بيانات القياس (Telemetry) - 73 بايت

### المواصفات الفنية
| المعيار | القيمة |
|---------|--------|
| **حجم الحزمة** | 73 بايت |
| **معدل البود** | 115200 bps |
| **المحول** | FTDI USB-Serial (VID: 0x0403) |
| **معدل الإرسال** | 60 Hz |
| **ترتيب البايتات** | Little Endian |

### جدول هيكل الحزمة الكامل
| الموقع | الحقل | الحجم | النوع | الوصف |
|--------|-------|-------|-------|-------|
| **0** | Header 1 | 1B | byte | `0xAA` |
| **1** | Header 2 | 1B | byte | `0x55` |
| **2** | Length | 1B | uint8 | `70` (الطول بدون الهيدر) |
| **3-6** | Timestamp | 4B | uint32 | الوقت بالميلي ثانية |
| **7-12** | Orientation | 6B | int16[3] | Roll, Pitch, Yaw (درجة × 10) |
| **13-18** | Acceleration | 6B | int16[3] | X, Y, Z (م/ث² × 100) |
| **19-22** | Pressure | 4B | mixed | الضغط (hPa) + ارتفاع بارو (م × 10) |
| **23-40** | GPS Data | 18B | mixed | Lat/Lon/Alt/Spd/Hdg/Sats/Fix/HDOP |
| **41-48** | Servo Cmds | 8B | int16[4] | أوامر S1-S4 (درجة × 10) |
| **49-56** | Servo FB | 8B | int16[4] | فيدباك S1-S4 (درجة × 10) |
| **57** | Servo Status | 1B | bitmask | bit0=S1 online, etc. |
| **58-65** | Tracking | 8B | mixed | Target X, Y, Width, Height |
| **66-69** | Battery | 4B | mixed | %, Charging, Voltage (mV) |
| **70-71** | Temperature | 2B | int16 | سيلسيوس × 10 |
| **72** | Checksum | 1B | byte | XOR لجميع البايتات 0-71 |

### خوارزمية الـ Checksum
```kotlin
fun calculateChecksum(data: ByteArray): Byte {
    var checksum: Byte = 0
    for (i in 0 until data.size - 1) {
        checksum = (checksum.toInt() xor data[i].toInt()).toByte()
    }
    return checksum
}
```

### مثال على حزمة كاملة
```
AA 55 46                              // Header + Length
00 00 03 E8                           // Timestamp: 1000ms
00 64 FF 9C 00 00                     // Roll:10°, Pitch:-10°, Yaw:0°
00 00 03 E8 26 AC                     // Accel: 0, 10, 98.04 (gravity)
03 F5 00 C8                           // Pressure: 1013hPa, Alt: 20m
...                                   // GPS + Servos + Tracking
25                                    // Checksum ✓
```

---

## 3.4 أوامر الطيران (STM32 Flight - 10 بايت)

### المواصفات الفنية
| المعيار | القيمة |
|---------|--------|
| **حجم الحزمة** | 10 بايت |
| **معدل الإرسال** | 50 Hz |
| **الاتصال** | USB CDC Serial |
| **ترتيب البايتات** | Little Endian |

### هيكل الحزمة
| البايت | الحقل | النوع | النطاق | الوصف |
|--------|-------|-------|--------|-------|
| 0 | Header 1 | byte | - | `0xAA` |
| 1 | Header 2 | byte | - | `0x08` |
| 2-3 | Throttle | uint16 | 0-1000 | قوة الدفع |
| 4-5 | Roll | int16 | ±500 | الميلان يمين/يسار |
| 6-7 | Pitch | int16 | ±500 | الميلان أمام/خلف |
| 8-9 | Yaw | int16 | ±500 | الدوران |

### آلية الاكتشاف (Handshake)
```
Android → STM32: "ID?" (0x49 0x44 0x3F)
STM32 → Android: "OK_STM32_REV1" (خلال 200ms)
```

### مثال
```kotlin
// طيران للأمام بـ 50% قوة
val packet = byteArrayOf(
    0xAA.toByte(), 0x08,        // Header
    0xF4, 0x01,                 // Throttle: 500
    0x00, 0x00,                 // Roll: 0
    0xF4, 0x00,                 // Pitch: 244 (forward)
    0x00, 0x00                  // Yaw: 0
)
```

---

## 3.5 واجهة العرض (HUD)

### عناصر العرض
| العنصر | الوصف | معدل التحديث |
|--------|-------|--------------|
| **Crosshair** | شعيرات التصويب | 60 FPS |
| **Target Box** | مربع الهدف + معرف التتبع | 30 FPS |
| **Confidence Bar** | نسبة الثقة بالهدف | 30 FPS |
| **Status Text** | ACQUIRING/TRACKING/ENGAGED | حسب الحدث |
| **Telemetry Overlay** | GPS/Battery/FPS | 1-10 Hz |
| **Threat Level** | مستوى التهديد (CRITICAL/HIGH/MEDIUM/LOW) | حسب الحدث |

### بنية بيانات التتبع
```kotlin
data class TrackingResult(
    val targetId: Int,           // معرف الهدف الفريد
    val boundingBox: RectF,      // إحداثيات المربع
    val confidence: Float,       // 0.0 - 1.0
    val className: String,       // "tank", "vehicle", etc.
    val state: TrackState,       // ACQUIRING, TRACKING, ENGAGED
    val threatLevel: ThreatLevel // CRITICAL, HIGH, MEDIUM, LOW
)
```

---

## 3.6 تسجيل البيانات (Data Logging)

### ملفات CSV - GPS
| الحقل | النوع | الوصف |
|-------|-------|-------|
| timestamp | string | الوقت بصيغة ISO 8601 |
| latitude | double | خط العرض |
| longitude | double | خط الطول |
| altitude | double | الارتفاع (متر) |
| speed | float | السرعة (م/ث) |
| bearing | float | الاتجاه (درجة) |
| accuracy | float | الدقة (متر) |
| satellites | int | عدد الأقمار |
| hdop | float | HDOP |
| vdop | float | VDOP |
| fix_type | int | نوع الـ Fix |
| source | string | "PHONE" أو "EXTERNAL" |

### مسار الحفظ
```
/storage/emulated/0/Download/
├── GPS_External_2026-01-12_163000.csv
├── GPS_Phone_2026-01-12_163000.csv
└── Mission_Recording_2026-01-12.mp4
```

---

# الفصل 4: البروتوكولات التفصيلية

## 4.1 بروتوكول CAN Bus (Waveshare USB-CAN-A)

### الطبقة الفيزيائية
| المعيار | القيمة |
|---------|--------|
| **المحول** | Waveshare USB-CAN-A |
| **VID** | `0x1A86` (CH340) |
| **سرعة CAN** | 2 Mbps |
| **إنهاء الخط** | 120Ω على طرفي الباص |

### تغليف الإطار (Waveshare Binary)
```
[HEAD: 0xAA][TYPE][ID_LOW][ID_HIGH][DLC][DATA_0...7][CHECKSUM]
```

| البايت | الوصف |
|--------|-------|
| HEAD | `0xAA` ثابت |
| TYPE | `0xC0` (Standard) أو `0xC1` (Extended) |
| ID | معرف CAN (11-bit أو 29-bit) |
| DLC | طول البيانات (0-8) |
| DATA | البيانات (حتى 8 بايت) |
| CHECKSUM | مجموع جميع البايتات mod 256 |

### أوامر XQPOWER v022
```kotlin
object CANServoProtocol {
    const val CMD_SET_POSITION = 0x01
    const val CMD_SET_VELOCITY = 0x02
    const val CMD_READ_FEEDBACK = 0x05
    
    // تحويل الزاوية: -100° to +100° → int16 × 10
    fun angleToRaw(angle: Float): Short = (angle * 10).toInt().toShort()
}
```

---

## 4.2 بروتوكول Serial الموحد (STM32 5-Byte)

### معرفات الأجهزة المدعومة
| الشريحة | VID | PID |
|---------|-----|-----|
| CH340 | 0x1A86 | 0x7523 |
| FTDI | 0x0403 | 0x6001 |
| CP2102 | 0x10C4 | 0xEA60 |
| STM32 | 0x0483 | 0x5740 |
| Arduino | 0x2341 | متعدد |

### خوارزمية بناء الحزمة
```kotlin
object UnifiedProtocol {
    const val OPCODE_MOVE = 0x08
    const val OPCODE_READ = 0x00
    const val NEUTRAL_POSITION = 8191
    const val UNITS_PER_DEGREE = 40.0f
    
    fun buildMoveCommand(servoId: Int, angleDegrees: Float): ByteArray {
        val position = ((angleDegrees * UNITS_PER_DEGREE) + NEUTRAL_POSITION)
            .toInt().coerceIn(0, 16383)
        
        val syncId = (0x80 or OPCODE_MOVE or ((servoId shr 7) and 0x03)).toByte()
        val id = (servoId and 0x7F).toByte()
        val hpos = ((position shr 7) and 0x7F).toByte()
        val lpos = (position and 0x7F).toByte()
        val checksum = ((syncId.toInt() xor id.toInt() xor 
                         hpos.toInt() xor lpos.toInt()) and 0x7F).toByte()
        
        return byteArrayOf(syncId, id, hpos, lpos, checksum)
    }
    
    fun buildReadCommand(servoId: Int): ByteArray {
        val syncId = (0x80 or OPCODE_READ or ((servoId shr 7) and 0x03)).toByte()
        val id = (servoId and 0x7F).toByte()
        val checksum = ((syncId.toInt() xor id.toInt()) and 0x7F).toByte()
        
        return byteArrayOf(syncId, id, 0x00, 0x00, checksum)
    }
}
```

### توقيت Round-Robin
```
Servo 1: [Command 5B] → [Read Req 5B] → [Wait 2ms] → [Read 7B]
Servo 2: [Command 5B] → [Read Req 5B] → [Wait 2ms] → [Read 7B]
Servo 3: [Command 5B] → [Read Req 5B] → [Wait 2ms] → [Read 7B]
────────────────────────────────────────────────────────────────
Total Cycle: ~10ms (100Hz loop → ~50Hz per-servo reporting)
```

---

## 4.3 بروتوكول القياس (73-Byte Telemetry)

### إنشاء حزمة القياس
```kotlin
class TelemetryStreamer {
    private val buffer = ByteBuffer.allocate(73)
        .order(ByteOrder.LITTLE_ENDIAN)
    
    fun buildPacket(
        orientation: FloatArray,     // Roll, Pitch, Yaw (degrees)
        acceleration: FloatArray,    // X, Y, Z (m/s²)
        gps: GPSData,
        servoCmds: FloatArray,       // 4 servo targets
        servoFB: FloatArray,         // 4 servo feedbacks
        tracking: TrackingResult?,
        battery: BatteryInfo
    ): ByteArray {
        buffer.clear()
        
        // Header
        buffer.put(0xAA.toByte())
        buffer.put(0x55.toByte())
        buffer.put(70.toByte())
        
        // Timestamp
        buffer.putInt((System.currentTimeMillis() % Int.MAX_VALUE).toInt())
        
        // Orientation (degrees × 10)
        buffer.putShort((orientation[0] * 10).toInt().toShort())
        buffer.putShort((orientation[1] * 10).toInt().toShort())
        buffer.putShort((orientation[2] * 10).toInt().toShort())
        
        // Acceleration (m/s² × 100)
        buffer.putShort((acceleration[0] * 100).toInt().toShort())
        buffer.putShort((acceleration[1] * 100).toInt().toShort())
        buffer.putShort((acceleration[2] * 100).toInt().toShort())
        
        // ... GPS, Servos, Tracking, Battery ...
        
        // Checksum
        val data = buffer.array()
        data[72] = calculateChecksum(data)
        
        return data
    }
}
```

---

## 4.4 بروتوكول KCA GPS

### خوارزمية CRC-16 CCITT
```kotlin
fun crc16CCITT(data: ByteArray, offset: Int, length: Int): Int {
    var crc = 0
    for (i in offset until offset + length) {
        crc = crc xor (data[i].toInt() and 0xFF shl 8)
        for (j in 0 until 8) {
            crc = if (crc and 0x8000 != 0) {
                (crc shl 1) xor 0x1021
            } else {
                crc shl 1
            }
        }
    }
    return crc and 0xFFFF
}
```

### تحليل حزمة KCA
```kotlin
class KcaParser {
    fun parse(data: ByteArray): KcaNavData? {
        // التحقق من الهيدر
        if (data[0] != 0x81.toByte() || data[1] != 0x7E.toByte()) {
            return null
        }
        
        // التحقق من CRC
        val payload = data.sliceArray(2..161)
        val expectedCRC = ((data[162].toInt() and 0xFF) shl 8) or 
                          (data[163].toInt() and 0xFF)
        if (crc16CCITT(payload, 0, 160) != expectedCRC) {
            return null
        }
        
        // استخراج البيانات
        val buffer = ByteBuffer.wrap(payload).order(ByteOrder.LITTLE_ENDIAN)
        return KcaNavData(
            state = buffer.get(1).toInt(),
            latitude = buffer.getFloat(72),
            longitude = buffer.getFloat(76),
            altitude = buffer.getFloat(80),
            // ...
        )
    }
}
```

---

# الفصل 5: مخططات التدفق

## 5.1 مخطط تدفق المدخلات

```
┌─────────────────────────────────────────────────────────────────┐
│                         المدخلات                                 │
└─────────────────────────────────────────────────────────────────┘
        │
        ├──────────────┬──────────────┬──────────────┬────────────┐
        ▼              ▼              ▼              ▼            ▼
┌──────────────┐┌──────────────┐┌──────────────┐┌──────────┐┌──────────┐
│   📷 Camera  ││  🧭 IMU      ││  📍 GPS      ││ ⚙️ Servo ││ 👆 User  │
│   CameraX    ││  Sensors     ││  Location    ││ Feedback ││ Touch    │
│   1080p 30Hz ││  100+ Hz     ││  1-10 Hz     ││ 50-100Hz ││ Events   │
└──────┬───────┘└──────┬───────┘└──────┬───────┘└────┬─────┘└────┬─────┘
       │               │               │             │           │
       ▼               ▼               ▼             ▼           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    طبقة دمج الحساسات                             │
│            Camera 70% + IMU 20% + GPS 10%                       │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │   FusedSensorData   │
                    └─────────────────────┘
```

## 5.2 مخطط تدفق المخرجات

```
┌─────────────────────────────────────────────────────────────────┐
│                        المعالجة                                   │
│              (Detection → Tracking → Decision)                   │
└──────────────────────────────┬──────────────────────────────────┘
                               │
       ┌───────────────────────┼───────────────────────┐
       │                       │                       │
       ▼                       ▼                       ▼
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│  ⚙️ Servos   │       │  📡 Telemetry│       │  🖥️ HUD     │
│  CAN/Serial  │       │  73-byte     │       │  Display    │
│  50 Hz       │       │  60 Hz       │       │  30-60 FPS  │
└──────┬───────┘       └──────┬───────┘       └──────┬───────┘
       │                      │                      │
       ▼                      ▼                      ▼
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│ Gimbal/Motor │       │ Python GCS   │       │ Operator     │
│ (Physical)   │       │ Viewer       │       │ Interface    │
└──────────────┘       └──────────────┘       └──────────────┘
```

## 5.3 مخطط التكامل الكلي

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          SAQR SEEKER SYSTEM                             │
└─────────────────────────────────────────────────────────────────────────┘

  ┌─────────────────┐                              ┌─────────────────┐
  │  📷 Camera      │                              │  ⚙️ Servos      │
  │  (1080p 30Hz)   │──┐                       ┌──▶│  (CAN/Serial)   │
  └─────────────────┘  │                       │   └─────────────────┘
                       │                       │
  ┌─────────────────┐  │   ┌───────────────┐   │   ┌─────────────────┐
  │  🧭 IMU         │──┼──▶│               │───┼──▶│  📡 Telemetry   │
  │  (100+ Hz)      │  │   │   ANDROID     │   │   │  (73-byte 60Hz) │
  └─────────────────┘  │   │    BRAIN      │   │   └─────────────────┘
                       │   │               │   │
  ┌─────────────────┐  │   │  • YOLOv8     │   │   ┌─────────────────┐
  │  📍 GPS         │──┼──▶│  • ByteTrack  │───┼──▶│  🖥️ HUD        │
  │  (Int+External) │  │   │  • Kalman     │   │   │  (30-60 FPS)    │
  └─────────────────┘  │   │  • PID        │   │   └─────────────────┘
                       │   │  • Decision   │   │
  ┌─────────────────┐  │   └───────────────┘   │   ┌─────────────────┐
  │  👆 User Input  │──┘           │           └──▶│  📹 Recording   │
  │  (Touch)        │              │               │  (MP4 + CSV)    │
  └─────────────────┘              │               └─────────────────┘
                                   │
                                   ▼
                          ┌───────────────┐
                          │  ✈️ STM32     │
                          │  Flight Ctrl  │
                          │  (10-byte)    │
                          └───────────────┘
```

---

# الملاحق

## ملحق أ: جدول معرفات USB المدعومة

| الجهاز | VID | PID | الاستخدام |
|--------|-----|-----|-----------|
| **Waveshare CAN** | 0x1A86 | 0x7523 | CAN Bus للسيرفوهات |
| **FTDI** | 0x0403 | 0x6001 | Telemetry Radio |
| **CP2102** | 0x10C4 | 0xEA60 | Serial Servos |
| **STM32 VCP** | 0x0483 | 0x5740 | Flight Controller |
| **CH340** | 0x1A86 | 0x7523 | Multi-purpose |

## ملحق ب: إعدادات Serial المعيارية

| الاستخدام | Baud Rate | Data Bits | Stop Bits | Parity |
|-----------|-----------|-----------|-----------|--------|
| Servos | 115200 | 8 | 1 | None |
| Telemetry | 115200 | 8 | 1 | None |
| CAN Adapter | 2000000 | 8 | 1 | None |
| KCA GPS | 115200 | 8 | 1 | None |

## ملحق ج: رموز الأخطاء

| الرمز | الوصف | الإجراء |
|-------|-------|---------|
| `E001` | فشل اتصال USB | تحقق من الكابل والصلاحيات |
| `E002` | فشل CRC | أعد المحاولة أو تحقق من الأسلاك |
| `E003` | Servo Timeout | تحقق من التوصيل والطاقة |
| `E004` | GPS No Fix | انتظر أو انتقل لمكان مفتوح |
| `E005` | Camera Error | أعد تشغيل التطبيق |

## ملحق د: متطلبات الأجهزة

### الحد الأدنى
- **الهاتف**: Android 9.0+، USB OTG
- **المحول**: Waveshare USB-CAN-A
- **الطاقة**: 5V/2A خارجي للسيرفوهات

### الموصى به
- **الهاتف**: Samsung S23 Ultra أو ما يعادله
- **المعالج**: Snapdragon 8 Gen 2+
- **الذاكرة**: 8GB+ RAM

---

# نهاية الدليل

**الإصدار**: 1.0  
**آخر تحديث**: 2026-01-12  
**المؤلف**: SAQR Seeker Development Team
